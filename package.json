{
  "name": "npm_test",
  "version": "1.0.0",
  "description": "this is test.",
  "main": "index.js",
  "engines": {
    "node": "12.16.2",
    "npm": "5.7.1"
  },
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "dependencies": {
    "axios": "^0.20.0",
    "bootstrap": "^3.4.1",
    "react-addons-update": "^15.6.2",
    "react-bootstrap": "^1.3.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "echo b && cp -a crud_front/build/. public/",
    "heroku-postbuild": "cd crud_front && echo before install && npm install && echo after install cd .."
  },
  "author": "ogawa",
  "license": "ISC"
}
